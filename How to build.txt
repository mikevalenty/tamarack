In order to build this project, you need to execute the build.cmd from the (PowerShell) console.
You do this using the following command from the root project directory:

	build.cmd

In order to create a nuget package, you need to pass the appropriate target as parameter

	build.cmd package